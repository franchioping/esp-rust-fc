[package]
name = "flight-control"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib"]

[dependencies]
nalgebra = { version = "0.34", default-features = false, features = [
	"libm",
	"macros",
] }


[features]
default = []
std = []
